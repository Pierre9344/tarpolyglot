% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tar-target-py.R
\name{tar_target_py_raw}
\alias{tar_target_py_raw}
\title{Target that runs a Python script (raw / factory form)}
\usage{
tar_target_py_raw(
  name,
  script,
  pre_script = NULL,
  post_script = NULL,
  inputs = NULL,
  output = "object",
  retrieve = NULL,
  files = NULL,
  python_version = NULL,
  env = NULL,
  env_manager = "system",
  python = NULL,
  pattern = NULL,
  packages = targets::tar_option_get("packages"),
  library = targets::tar_option_get("library"),
  deps = NULL,
  string = NULL,
  format = NULL,
  repository = targets::tar_option_get("repository"),
  iteration = targets::tar_option_get("iteration"),
  error = targets::tar_option_get("error"),
  memory = targets::tar_option_get("memory"),
  garbage_collection = isTRUE(targets::tar_option_get("garbage_collection")),
  deployment = targets::tar_option_get("deployment"),
  priority = targets::tar_option_get("priority"),
  resources = targets::tar_option_get("resources"),
  storage = targets::tar_option_get("storage"),
  retrieval = targets::tar_option_get("retrieval"),
  cue = targets::tar_option_get("cue"),
  description = targets::tar_option_get("description")
)
}
\arguments{
\item{name}{Character string, the target name.}

\item{script}{Path to the Python script to run (required). Either a literal string (an untracked path: editing the file does not invalidate the target) or a \code{\link[=tar_target_path]{tar_target_path()}} reference to an upstream target (typically \code{format = "file"}), which makes this step re-run whenever that file changes.}

\item{pre_script}{Optional path to an R script run before the Python script. See \code{\link[=run_py_step]{run_py_step()}}; assign a named list \code{to_py} to hand objects to Python. Accepts a literal string or a \code{\link[=tar_target_path]{tar_target_path()}} reference, as for \code{script}.}

\item{post_script}{Optional path to an R script run after the Python script. See \code{\link[=run_py_step]{run_py_step()}}; helpers \code{py} and \code{py_get()} are available, and its last expression (object mode) or returned paths (file mode) become the value. Accepts a literal string or a \code{\link[=tar_target_path]{tar_target_path()}} reference, as for \code{script}.}

\item{inputs}{Named character vector (or list) mapping the name seen inside the step (in the R environment and, after the hand-off, in the foreign session) to the name of an upstream target, e.g. \code{c(x = "prepared_x")}. Each upstream target becomes a dependency of this target and is bound by that name in the step environment; under dynamic branching the per-branch slice is bound instead.}

\item{output}{Output mode: \code{"object"} (default) returns a converted R object, \code{"file"} returns a character vector of file paths (and defaults \code{format} to \code{"file"}).}

\item{retrieve}{Optional character vector of foreign-session variable names to return when no \code{post_script} is supplied in object mode. One name returns that object; several return a named list.}

\item{files}{Optional character vector of file paths to return when no \code{post_script} is supplied in file mode.}

\item{python_version}{Optional Python version to select (e.g. \code{"3.12"} or \code{">=3.11"}), used only when no environment and no explicit \code{python} path are given. reticulate fetches/selects it (via its uv-backed ephemeral environment) with \code{\link[reticulate:py_require]{reticulate::py_require()}}. Default \code{NULL} uses the computer/global default Python. Use this when you only care about the interpreter \emph{version} and are happy for reticulate to build a \emph{throwaway} environment; for a \emph{pinned, reproducible} environment use \code{env} / \code{env_manager} (or \code{python}) instead.}

\item{env, env_manager, python}{Python environment selection: the reproducible alternatives to \code{python_version}. Use \code{env} + \code{env_manager} to point at an existing environment built by a known tool, or \code{python} for one explicit interpreter path. \code{env_manager} is one of \code{"system"}, \code{"virtualenv"}, \code{"venv"}, \code{"conda"}, \code{"uv"}, \code{"poetry"}; \code{env} is the corresponding virtualenv/conda name or path (or poetry project directory); \code{python} is an explicit interpreter path. Precedence: \code{python} > environment (\code{env}/\code{env_manager}) > \code{python_version} > default. (\code{"virtualenv"}, \code{"venv"} and \code{"uv"} all point at a standard virtualenv, including one created by \code{renv::use_python()}, and behave identically.) For \code{"virtualenv"}/\code{"venv"}/\code{"uv"}/\code{"poetry"}, an \code{env} that is an already-existing directory (relative to the working directory, or absolute) is resolved to an absolute path before use, so a relative venv path (e.g. \code{".venv"}, created with \verb{uv venv .venv}) works as expected; otherwise \code{reticulate::use_virtualenv()} would misread a separator-less relative path as the \emph{name} of an environment under its own virtualenv root instead of a path on disk. A bare name that does not correspond to an existing directory is passed through unchanged, so a genuine named environment (one already registered under that root) still resolves. When \code{python} or an environment is given, the selection also takes priority over an ambient \code{RETICULATE_PYTHON} environment variable (e.g. one set by the RStudio project Python config and inherited by \code{crew} workers), which reticulate would otherwise let silently override the request.}

\item{pattern}{Optional \pkg{targets} dynamic-branching pattern as a language object (e.g. \code{quote(map(x))}), forwarded to \code{\link[targets:tar_target_raw]{targets::tar_target_raw()}}.}

\item{packages, library}{Character vectors of R packages (and library paths) to load for the target, forwarded to \code{\link[targets:tar_target_raw]{targets::tar_target_raw()}}.}

\item{deps, string}{Advanced \code{\link[targets:tar_target_raw]{targets::tar_target_raw()}} arguments: extra dependency names and a string used for change detection.}

\item{format, repository, iteration}{Storage/iteration settings forwarded to \code{\link[targets:tar_target_raw]{targets::tar_target_raw()}}. \code{format} defaults to \code{"file"} when \code{output = "file"}, otherwise to the \code{targets} option default.}

\item{error, memory, garbage_collection, deployment, priority, resources, storage, retrieval, cue, description}{Standard \code{\link[targets:tar_target_raw]{targets::tar_target_raw()}} execution/behaviour arguments, forwarded unchanged.}
}
\value{
A \code{targets} target object.
}
\description{
Character-based constructor mirroring \code{\link[targets:tar_target_raw]{targets::tar_target_raw()}}: \code{name} is a string and it is meant for use \emph{inside targets factories}. Returns a single \code{targets} target whose command runs an optional R pre-script, \code{script} (via \pkg{reticulate}), and an optional R post-script, then returns a converted R object or a character vector of files. See \code{\link[=run_py_step]{run_py_step()}} for the pre/post-script contract (the \code{to_py} hand-off and the \code{py} / \code{py_get} helpers). For direct use in \verb{_targets.R}, see \code{\link[=tar_target_py]{tar_target_py()}}. The Julia twin is \code{\link[=tar_target_jl_raw]{tar_target_jl_raw()}}.
}
\details{
All \code{\link[targets:tar_target_raw]{targets::tar_target_raw()}} arguments are forwarded unchanged, so dynamic branching (\code{pattern}), storage \code{format}, \code{deployment}, \code{resources}, \code{cue}, etc. all work as usual. Upstream targets are wired in through \code{inputs}, which become dependencies (and are sliced under dynamic branching).

The interpreter/environment selection arguments (\code{python}, \code{env}, \code{env_manager}, \code{python_version}) are forwarded to \code{\link[=run_py_step]{run_py_step()}}, which documents them (they are \emph{alternatives}: normally set only one; precedence \code{python} > \code{env}/\code{env_manager} > \code{python_version} > none). See also \code{vignette("python")} for a decision guide with examples.
}
\examples{
\dontrun{
# Inside a targets factory:
tarpolyglot::tar_target_py_raw(
  name = "py_sum",
  script = "scripts/sum.py",
  inputs = c(x = "prepared_x"),
  pre_script = "scripts/pre.R",   # builds `to_py <- list(x = x)`
  post_script = "scripts/post.R"  # ends on `py$result`
)
}
}
\seealso{
\code{\link[=tar_target_py]{tar_target_py()}}, \code{\link[=run_py_step]{run_py_step()}}, \code{\link[=tar_target_jl_raw]{tar_target_jl_raw()}}
}
