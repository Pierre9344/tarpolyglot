% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shared-params.R
\name{tarpolyglot-post-script-compiled}
\alias{tarpolyglot-post-script-compiled}
\title{Post-script argument for the compiled-language constructors}
\arguments{
\item{post_script}{Path to an R script run after compilation, where the compiled functions and \code{inputs} are in scope. Its last expression is the value (object mode); it returns file paths (file mode). Required for object mode. Accepts a literal path, a \code{\link[=tar_target_path]{tar_target_path()}} reference, or inline code from \code{\link[=tar_code]{tar_code()}}; see the "Script options" section below.}
}
\description{
Rust and C++ share the same post-script contract: there is no pre-script and
no live interpreter, so the compiled functions are called from an R script
run after the build. Python and Julia differ (they have a pre-script and an
interpreter hand-off), so they keep their own wording.
}
\keyword{internal}
