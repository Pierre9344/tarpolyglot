% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run-step-rs.R
\name{run_rs_step}
\alias{run_rs_step}
\title{Execute a Rust step (worker behind tar_target_rs)}
\usage{
run_rs_step(
  script,
  post_script = NULL,
  inputs = list(),
  output = "object",
  files = NULL,
  dependencies = NULL,
  features = NULL,
  profile = NULL,
  toolchain = NULL
)
}
\arguments{
\item{script}{Path to the Rust script containing \verb{#[extendr]} functions (required). Accepts a file path or an inline \code{\link[=tar_code]{tar_code()}} carrier; see the "Script arguments" section below.}

\item{post_script}{Path to an R script evaluated after compilation. The compiled Rust functions and the named \code{inputs} are in scope; its last expression is the target value (object mode), or it returns a character vector of file paths (file mode). Required for object mode. Accepts a file path or an inline \code{\link[=tar_code]{tar_code()}} carrier; see the "Script arguments" section below.}

\item{inputs}{Named character vector (or list) mapping the name seen inside the step (in the R environment and, after the hand-off, in the foreign session) to the name of an upstream target, e.g. \code{c(x = "prepared_x")}. Each upstream target becomes a dependency of this target and is bound by that name in the step environment; under dynamic branching the per-branch slice is bound instead.}

\item{output}{Output mode: \code{"object"} (default) returns a converted R object, \code{"file"} returns a character vector of file paths (and defaults \code{format} to \code{"file"}).}

\item{files}{Optional character vector of file paths to return when no \code{post_script} is supplied in file mode.}

\item{dependencies, features, profile}{Passed to \code{\link[rextendr:rust_source]{rextendr::rust_source()}}: crate \code{dependencies} (named list), Cargo \code{features}, and build \code{profile} (e.g. \code{"dev"} or \code{"release"}).}

\item{toolchain}{Optional rustup toolchain (e.g. \code{"stable-x86_64-pc-windows-gnu"}); sets \code{RUSTUP_TOOLCHAIN} for the build. Default \code{NULL} uses the rustup default toolchain.}
}
\value{
The value of the post-script (object mode) or a character vector of normalised file paths (file mode).
}
\description{
Compiles the \verb{#[extendr]} functions in a Rust script with \code{\link[rextendr:rust_source]{rextendr::rust_source()}}, exposing them as R functions in a fresh environment, then evaluates an R \strong{post-script} in that environment where you call those functions and return the result. Upstream \code{inputs} are bound in the same environment. This is the function the target built by \code{\link[=tar_target_rs]{tar_target_rs()}} calls; it is exported so the call resolves at run time, but package users should not call it directly.
}
\details{
Unlike Python/Julia there is \strong{no pre-script} for Rust and no live interpreter: \code{rust_source()} compiles a dynamic library and R calls into it with real type conversion (via \href{https://extendr.rs/}{extendr}). A Rust toolchain and \code{cargo} must be reachable (this function puts R, cargo, and on Windows Rtools on \code{PATH} for the build itself); on Windows use the GNU toolchain.
}
\section{Script arguments}{


A worker receives whatever the constructor already resolved, which is one of two things: a \strong{path to a file} on disk, or an \strong{inline carrier} built by \code{\link[=tar_code]{tar_code()}} that holds the code in memory. Both are accepted, so a direct call may pass either.

\code{\link[=tar_target_path]{tar_target_path()}} is deliberately \emph{not} a third form at this level. It is a constructor-level convenience: \code{\link[=tar_target_py]{tar_target_py()}} and the other constructors rewrite it while the pipeline's DAG is built, so that by the time a worker runs it has already become the ordinary file path held by the upstream target. Handing the result of \code{\link[=tar_target_path]{tar_target_path()}} straight to a worker therefore does not resolve to a file. The three forms as written in \verb{_targets.R}, and which of them tracks your edits, are covered in \code{vignette("scripts")}.
}

\examples{
\dontrun{
# Normally invoked by tar_target_rs(); shown here as a direct call.
# scripts/square.rs:
#   #[extendr]
#   fn square(x: f64) -> f64 { x * x }
# scripts/post.R:
#   square(x)
run_rs_step(
  script = "scripts/square.rs",
  inputs = list(x = 21),
  post_script = "scripts/post.R"
)
}
}
\seealso{
\code{\link[=tar_target_rs]{tar_target_rs()}}, \code{\link[=run_py_step]{run_py_step()}}, \code{\link[=run_jl_step]{run_jl_step()}}
}
