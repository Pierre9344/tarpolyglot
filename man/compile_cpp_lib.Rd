% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run-step-cpp.R
\name{compile_cpp_lib}
\alias{compile_cpp_lib}
\title{Compile a C++ step once for reuse across branches (worker behind tarpolyglot_map)}
\usage{
compile_cpp_lib(script, depends = NULL)
}
\arguments{
\item{script}{Path to the C++ script containing \verb{// [[Rcpp::export]]} functions (required). Accepts a file path or an inline \code{\link[=tar_code]{tar_code()}} carrier; see the "Script arguments" section below.}

\item{depends}{Optional character vector of extension packages (e.g. \code{c("RcppArmadillo", "RcppEigen")}), passed straight through as \verb{// [[Rcpp::depends(...)]]} would be. Usually unnecessary: declaring \verb{// [[Rcpp::depends(pkgname)]]} directly in the C++ source (the normal Rcpp convention) already works, so this argument is only useful if you would rather not repeat that in the source itself.}
}
\value{
An object of class \code{tp_cpp_lib}: a list with the library \code{basename}, the raw library \code{bytes}, the generated wrapper \code{wrapper_src} (character vector of R source lines), the \code{orig_path} the wrapper source's \code{dyn.load()} call originally pointed at (rewritten on reload), and \code{objs_names} (the exported function names, from \code{sourceCpp()}'s own \verb{$functions}).
}
\description{
Compiles the \verb{// [[Rcpp::export]]} functions in a C++ script with \code{\link[Rcpp:sourceCpp]{Rcpp::sourceCpp()}} and returns a self-contained bundle that \code{\link[=run_cpp_step_prebuilt]{run_cpp_step_prebuilt()}} can reload in any branch without recompiling. This is the function the companion \verb{<name>_cpp_lib} target built by \code{tar_target_cpp(..., pattern = tarpolyglot_map(...))} calls; it is exported so the call resolves at run time, but package users should not call it directly.
}
\details{
Unlike \code{\link[=compile_rs_lib]{compile_rs_lib()}}, the bundle does \strong{not} embed ready-to-call R closures: Rcpp's generated wrapper functions capture a raw compiled-routine pointer at the moment they are bound to a loaded library (\code{Rcpp:::sourceCppFunction()}, confirmed by inspecting the \code{.cpp.R} file \code{sourceCpp()} generates alongside the compiled library), unlike rextendr's wrappers, which resolve their routine by name from a named \verb{PACKAGE=} at \emph{call} time. A pointer captured in one process is meaningless in another (or even in the same process after the original library is unloaded), so a closure built here could not simply be reused after being carried to a different \code{crew} worker. Instead this bundle embeds the compiled library's bytes \emph{and} the generated R wrapper source text (with its \code{dyn.load()} call still pointing at this process's build path); \code{\link[=run_cpp_step_prebuilt]{run_cpp_step_prebuilt()}} rewrites that path to wherever it re-materialises the library and re-evaluates the wrapper source there, which re-binds fresh, valid closures in the new process without recompiling.
}
\section{Script arguments}{


A worker receives whatever the constructor already resolved, which is one of two things: a \strong{path to a file} on disk, or an \strong{inline carrier} built by \code{\link[=tar_code]{tar_code()}} that holds the code in memory. Both are accepted, so a direct call may pass either.

\code{\link[=tar_target_path]{tar_target_path()}} is deliberately \emph{not} a third form at this level. It is a constructor-level convenience: \code{\link[=tar_target_py]{tar_target_py()}} and the other constructors rewrite it while the pipeline's DAG is built, so that by the time a worker runs it has already become the ordinary file path held by the upstream target. Handing the result of \code{\link[=tar_target_path]{tar_target_path()}} straight to a worker therefore does not resolve to a file. The three forms as written in \verb{_targets.R}, and which of them tracks your edits, are covered in \code{vignette("scripts")}.
}

\examples{
# Compiling needs a C++ compiler reachable by R, so this is gated on
# TARPOLYGLOT_EXAMPLES=true and runs in a temporary directory.
if (identical(Sys.getenv("TARPOLYGLOT_EXAMPLES"), "true")) {
  targets::tar_dir({
    # Normally invoked by tar_target_cpp(pattern = tarpolyglot_map(...)).
    writeLines(
      c("#include <Rcpp.h>",
        "// [[Rcpp::export]]",
        "double square(double x) { return x * x; }"),
      "square.cpp"
    )
    lib <- compile_cpp_lib(script = "square.cpp")
    class(lib)
  })
}
}
\seealso{
\code{\link[=tarpolyglot_map]{tarpolyglot_map()}}, \code{\link[=run_cpp_step_prebuilt]{run_cpp_step_prebuilt()}}, \code{\link[=tar_target_cpp]{tar_target_cpp()}}
}
\keyword{internal}
