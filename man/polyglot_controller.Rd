% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crew.R
\name{polyglot_controller}
\alias{polyglot_controller}
\title{A crew controller preconfigured for polyglot isolation}
\usage{
polyglot_controller(
  workers = 2L,
  tasks_max = 1L,
  seconds_idle = 30,
  log = NULL,
  ...
)
}
\arguments{
\item{workers}{Integer number of parallel worker processes. Default \code{2}.}

\item{tasks_max}{Integer max tasks a worker runs before it is retired (and its embedded interpreter torn down). Default \code{1} for maximum isolation: a fresh Python/Julia per target. Raise it (or set \code{Inf}) to reuse interpreters for throughput, accepting that same-language steps on a worker then share the foreign global namespace and one environment.}

\item{seconds_idle}{Numeric seconds an idle worker waits before shutting down. Default \code{30}.}

\item{log}{Optional \code{\link[=tar_polyglot_log]{tar_polyglot_log()}} object turning on per-step stdout/stderr log files for Python and Julia steps (see that function; Rust is not covered). Default \code{NULL} (no logging, the previous behavior).}

\item{...}{Further arguments passed to \code{\link[crew:crew_controller_local]{crew::crew_controller_local()}}, e.g. \code{seconds_timeout}, \code{garbage_collection}, or \code{reset_globals}.}
}
\value{
A \code{crew} controller object, ready for \code{targets::tar_option_set(controller = ...)}.
}
\description{
Returns a \code{\link[crew:crew_controller_local]{crew::crew_controller_local()}} tuned for pipelines that run Python or Julia steps. Because reticulate and JuliaCall embed a \strong{single interpreter per process} (see \code{vignette("get_started", package = "tarpolyglot")}), the only way to get a fresh interpreter per target, and to use different environments/versions across targets, is process-level isolation. Passing this controller to \code{\link[targets:tar_option_set]{targets::tar_option_set()}} sends targets to separate worker processes; with \code{tasks_max = 1} each worker is retired after one task, so every foreign step gets a clean interpreter that is torn down when the task finishes.
}
\details{
This is the recommended default for polyglot pipelines. Put it near the top of \verb{_targets.R}:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{targets::tar_option_set(controller = tarpolyglot::polyglot_controller())
}\if{html}{\out{</div>}}
}
\examples{
\dontrun{
# _targets.R
library(targets)
library(tarpolyglot)
tar_option_set(controller = polyglot_controller(workers = 4))
list(
  tar_target_py(x, "scripts/step.py", retrieve = "result")
)
}
}
\seealso{
\code{\link[=tar_target_py]{tar_target_py()}}, \code{\link[=tar_target_jl]{tar_target_jl()}}, \code{\link[=tar_polyglot_log]{tar_polyglot_log()}}
}
