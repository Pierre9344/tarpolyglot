% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/toolchain-check.R
\name{toolchain_check}
\alias{toolchain_check}
\title{Diagnose the Python, Julia, Rust, and C++ toolchains}
\usage{
toolchain_check(
  toolchains = c("py", "jl", "rs", "cpp"),
  deep = TRUE,
  quiet = FALSE
)
}
\arguments{
\item{toolchains}{Character vector, which toolchains to check: any subset of \code{"py"}, \code{"jl"}, \code{"rs"}, \code{"cpp"}. Default checks all four.}

\item{deep}{Logical. When \code{TRUE} (the default), the Rust and C++ checks additionally compile a trivial function (\verb{#[extendr]} / \verb{// [[Rcpp::export]]}) in a fresh worker to prove the full toolchain actually links together end to end; this is the most informative check for each but also the slowest (well under a minute, but not instant). Set \code{deep = FALSE} to skip it and rely on the faster presence checks alone. Python and Julia's fresh-worker checks are cheap either way and always run.}

\item{quiet}{Logical, default \code{FALSE}. Suppress the live \code{cli} progress/result output; the return value is unaffected either way.}
}
\value{
Invisibly, a \code{data.frame} with one row per check and columns \code{toolchain} (\code{"py"}/\code{"jl"}/\code{"rs"}/\code{"cpp"}), \code{check} (a short label), \code{status} (\code{"ok"}, \code{"warn"}, or \code{"fail"}), and \code{detail} (a human-readable description, e.g. the resolved path and version, or an error message).
}
\description{
Runs a battery of checks for whichever toolchains you ask for and reports the result with \href{https://cli.r-lib.org/}{cli} as it goes: interpreter/compiler discovery, environment-manager availability, and, most importantly, whether each toolchain is actually \emph{reachable from a fresh worker process} -- the same kind of process \code{crew} spawns to run a \code{tar_target_py()}/\code{tar_target_jl()}/\code{tar_target_rs()} step. This is meant to preempt the common "it doesn't run on my machine" class of issue before you find out the hard way, mid-pipeline.
}
\details{
Environment managers checked per language: for Python, \code{uv}, \code{poetry}, and \code{conda} (three separate, competing tools, plus the stdlib \code{venv} module on the resolved interpreter). For Julia, \code{juliaup} (which manages \emph{versions}, checked via presence + the list of installed versions) and, separately, Julia's own \code{Pkg} project-environment mechanism (which manages \emph{packages}, activated with \code{Pkg.activate()} -- the actual mechanism behind \code{julia_project}/\code{julia_packages}): a fresh worker actually activates a throwaway project to prove it works, not just that Julia is present. Rust has no separate environment concept (see \code{vignette("rust")}); \code{rustup}/\code{cargo}/(on Windows) the GNU toolchain and Rtools are checked instead.

Every check that would bind an interpreter (Python, Julia) or compile code (Rust, C++) runs inside a disposable \code{\link[callr:r]{callr::r()}} subprocess, never in your current R session: this means \code{toolchain_check()} cannot leave reticulate or JuliaCall bound afterwards, and it is answering the \emph{real} question ("would a crew worker be able to do this right now"), not just "is something already loaded in this session". Plain presence checks (\code{Sys.which()} for \code{uv}, \code{poetry}, \code{rustup}, and so on, or \code{requireNamespace()} for Rcpp) run directly, since they have no side effects to isolate.

When more than one version is found, each language also reports every installed version it can enumerate, with its path and which one is the default (the one a step would actually use if you set nothing): Python via \verb{uv python list --only-installed} (matched against the resolved interpreter primarily by exact version, since reticulate's default is often an \emph{ephemeral} uv-provisioned interpreter that lives under uv's cache rather than its "installed" registry, so the two rarely share a literal path even when they are, in fact, the same build); Julia via the juliaup depot; Rust via \verb{rustup toolchain list -v} (which marks its own default inline, no cross-referencing needed). With only one version found, this is a single summary line instead. C++ has no separate version-manager concept to enumerate this way (see \code{tar_target_cpp()}): it compiles via R's own configured toolchain, so its checks are presence (Rcpp; the OS-appropriate compiler, Rtools on Windows or the actual \verb{R CMD config CXX} compiler command on macOS/Linux, queried rather than guessed so it is correct under gcc or clang alike) plus, with \code{deep = TRUE}, a compile-reachability check. Extension packages such as RcppArmadillo/RcppEigen/RcppParallel are not checked here since tarpolyglot itself does not depend on any of them; they are entirely the caller's own choice via \code{depends} (see \code{tar_target_cpp()}).
}
\examples{
\dontrun{
toolchain_check()                      # everything
toolchain_check("py")                  # Python only
toolchain_check(c("jl", "rs"))         # Julia and Rust
toolchain_check("rs", deep = FALSE)    # skip the Rust compile test
toolchain_check("cpp")                 # C++ only
res <- toolchain_check(quiet = TRUE)   # no console output, just the data.frame
res[res$status != "ok", ]              # anything that needs attention
}
}
\seealso{
\code{\link[=tar_target_py]{tar_target_py()}}, \code{\link[=tar_target_jl]{tar_target_jl()}}, \code{\link[=tar_target_rs]{tar_target_rs()}}, \code{\link[=tar_target_cpp]{tar_target_cpp()}}
}
