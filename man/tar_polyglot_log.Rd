% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/log.R
\name{tar_polyglot_log}
\alias{tar_polyglot_log}
\title{Configure per-step stdout/stderr logging}
\usage{
tar_polyglot_log(
  stdout = "./logs/out",
  stderr = "./logs/err",
  append = FALSE,
  header = TRUE
)
}
\arguments{
\item{stdout, stderr}{Directory to write per-step log files into (created if missing), or \code{NULL} to disable that stream. Default \code{"./logs/out"} / \code{"./logs/err"}.}

\item{append}{If \code{FALSE} (default), a step's log file is truncated at the start of that step's run, so it only ever holds the latest run's output. If \code{TRUE}, new output is appended after two blank lines separating it from any prior content, so the file accumulates history across repeated runs.}

\item{header}{If \code{TRUE} (default), the stdout file gets a header written before the step's own output: the step name, \code{date()}, the resolved interpreter's version and path, and whether an explicit environment was used.}
}
\value{
A list with class \code{tp_log}, for \code{polyglot_controller(log = ...)}.
}
\description{
Passed to \code{\link[=polyglot_controller]{polyglot_controller()}}'s \code{log} argument to write one log file per Python/Julia step: \verb{<name>.out} for stdout, \verb{<name>.err} for stderr, named after the target. \pkg{crew} launches worker processes before it knows which target they will run, so this configuration cannot be applied at worker-launch time; instead \code{polyglot_controller()} stashes it as environment variables (inherited by every worker process it spawns, the same way any other environment variable is), and \code{\link[=run_py_step]{run_py_step()}} / \code{\link[=run_jl_step]{run_jl_step()}} read them back and do the actual redirection once they know which step is running and what interpreter it resolved.
}
\details{
Rust steps (\code{\link[=tar_target_rs]{tar_target_rs()}}) are not covered: \pkg{rextendr}-compiled code writes straight to the OS file descriptor, bypassing the redirection mechanism reticulate/JuliaCall provide for their embedded interpreters. Use \pkg{crew}'s own \code{options_local(log_directory = ...)} (process-level logging; since \code{polyglot_controller()} defaults to \code{tasks_max = 1}, each worker runs exactly one step, so that already gives one log per step, Rust included) if you need Rust step output.
}
\examples{
\dontrun{
targets::tar_option_set(
  controller = tarpolyglot::polyglot_controller(
    log = tarpolyglot::tar_polyglot_log(stdout = "./logs/out", stderr = "./logs/err")
  )
)
}
}
\seealso{
\code{\link[=polyglot_controller]{polyglot_controller()}}
}
