% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run-step-cpp.R
\name{run_cpp_step_prebuilt}
\alias{run_cpp_step_prebuilt}
\title{Run a C++ step from a pre-compiled library (worker behind tarpolyglot_map)}
\usage{
run_cpp_step_prebuilt(
  lib,
  post_script = NULL,
  inputs = list(),
  output = "object",
  files = NULL,
  name = NULL
)
}
\arguments{
\item{lib}{A \code{tp_cpp_lib} bundle from \code{\link[=compile_cpp_lib]{compile_cpp_lib()}} (supplied by the companion \verb{<name>_cpp_lib} target).}

\item{post_script}{Path to an R script evaluated after compilation. The compiled C++ functions and the named \code{inputs} are in scope; its last expression is the target value (object mode), or it returns a character vector of file paths (file mode). Required for object mode. Accepts a file path or an inline \code{\link[=tar_code]{tar_code()}} carrier; see the "Script arguments" section below.}

\item{inputs}{Named character vector (or list) mapping the name seen inside the step (in the R environment and, after the hand-off, in the foreign session) to the name of an upstream target, e.g. \code{c(x = "prepared_x")}. Each upstream target becomes a dependency of this target and is bound by that name in the step environment; under dynamic branching the per-branch slice is bound instead.}

\item{output}{Output mode: \code{"object"} (default) returns a converted R object, \code{"file"} returns a character vector of file paths (and defaults \code{format} to \code{"file"}).}

\item{files}{Optional character vector of file paths to return when no \code{post_script} is supplied in file mode.}

\item{name}{Character string, the branch's target name. Supplied automatically by the constructor; used only to name this branch's log files when \code{\link[=polyglot_controller]{polyglot_controller()}} was given a \code{\link[=tar_polyglot_log]{tar_polyglot_log()}} (\code{NULL} -- the default -- disables logging for a direct call). Note this is the branch name, not \verb{<name>_cpp_lib} -- reloading a pre-compiled library never itself produces output to log.}
}
\value{
The value of the post-script (object mode) or a character vector of normalised file paths (file mode).
}
\description{
Reloads a compiled C++ library produced by \code{\link[=compile_cpp_lib]{compile_cpp_lib()}} (writing the embedded shared library to a temporary file, then re-evaluating the embedded wrapper source so it binds fresh, valid closures against \emph{this} process's copy -- see \code{\link[=compile_cpp_lib]{compile_cpp_lib()}} for why that is necessary, unlike Rust's \code{\link[=run_rs_step_prebuilt]{run_rs_step_prebuilt()}}), then evaluates the R \strong{post-script} exactly as \code{\link[=run_cpp_step]{run_cpp_step()}} does, with the compiled functions and the named \code{inputs} in scope. This is the function each branch target built by \code{tar_target_cpp(..., pattern = tarpolyglot_map(...))} calls; it is exported so the call resolves at run time, but package users should not call it directly.
}
\details{
No C++ toolchain is needed here: reloading does not compile anything. See \code{\link[=tarpolyglot_map]{tarpolyglot_map()}} for the overall design.
}
\section{Script arguments}{


A worker receives whatever the constructor already resolved, which is one of two things: a \strong{path to a file} on disk, or an \strong{inline carrier} built by \code{\link[=tar_code]{tar_code()}} that holds the code in memory. Both are accepted, so a direct call may pass either.

\code{\link[=tar_target_path]{tar_target_path()}} is deliberately \emph{not} a third form at this level. It is a constructor-level convenience: \code{\link[=tar_target_py]{tar_target_py()}} and the other constructors rewrite it while the pipeline's DAG is built, so that by the time a worker runs it has already become the ordinary file path held by the upstream target. Handing the result of \code{\link[=tar_target_path]{tar_target_path()}} straight to a worker therefore does not resolve to a file. The three forms as written in \verb{_targets.R}, and which of them tracks your edits, are covered in \code{vignette("scripts")}.
}

\examples{
# Reloading needs a library built by compile_cpp_lib(), which needs a C++
# compiler, so this is gated on TARPOLYGLOT_EXAMPLES=true.
if (identical(Sys.getenv("TARPOLYGLOT_EXAMPLES"), "true")) {
  targets::tar_dir({
    # Normally invoked by tar_target_cpp(pattern = tarpolyglot_map(...)).
    writeLines(
      c("#include <Rcpp.h>",
        "// [[Rcpp::export]]",
        "double square(double x) { return x * x; }"),
      "square.cpp"
    )
    writeLines("square(x)", "post.R")
    lib <- compile_cpp_lib(script = "square.cpp")
    run_cpp_step_prebuilt(lib = lib, inputs = list(x = 21), post_script = "post.R")
  })
}
}
\seealso{
\code{\link[=tarpolyglot_map]{tarpolyglot_map()}}, \code{\link[=compile_cpp_lib]{compile_cpp_lib()}}, \code{\link[=run_cpp_step]{run_cpp_step()}}
}
\keyword{internal}
