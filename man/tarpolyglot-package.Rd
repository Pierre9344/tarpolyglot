% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tarpolyglot-package.R
\docType{package}
\name{tarpolyglot-package}
\alias{tarpolyglot}
\alias{tarpolyglot-package}
\title{tarpolyglot: run Python, Julia, and Rust as targets pipeline steps}
\description{
\if{html}{\figure{logo.png}{options: style='float: right' alt='logo' width='120'}}
}
\details{
tarpolyglot provides target constructors that execute Python, Julia, and Rust code inside a \href{https://docs.ropensci.org/targets/}{targets} pipeline, using \pkg{reticulate} (Python), \pkg{JuliaCall} (Julia), and \pkg{rextendr} (Rust).

The user-facing constructors mirror the \pkg{targets} pair:
\itemize{
\item \code{\link[=tar_target_py]{tar_target_py()}} / \code{\link[=tar_target_py_raw]{tar_target_py_raw()}} for Python,
\item \code{\link[=tar_target_jl]{tar_target_jl()}} / \code{\link[=tar_target_jl_raw]{tar_target_jl_raw()}} for Julia,
\item \code{\link[=tar_target_rs]{tar_target_rs()}} / \code{\link[=tar_target_rs_raw]{tar_target_rs_raw()}} for Rust (compiled via rextendr/extendr; see \code{vignette("rust")}).
}

The non-\verb{_raw} forms use non-standard evaluation on \code{name}/\code{pattern} and are meant for direct use in \verb{_targets.R}; the \verb{_raw} forms take a string \code{name} and are meant for use inside targets factories. Each constructor returns a single \code{targets} target object.

A Python/Julia step may combine up to three scripts: an optional R \strong{pre-script} (prepare inputs), the required \strong{foreign script}, and an optional R \strong{post-script} (retrieve results). A Rust step has no pre-script: its \verb{#[extendr]} functions are compiled and then called from the R post-script. Output is returned either as a converted R object or as a character vector of files on disk (\code{output = "file"}).

Start with \code{vignette("get_started")} (motivation, limitations, a worked pipeline, and \code{crew}-based parallelism/isolation), then the per-language vignettes \code{vignette("python")}, \code{vignette("julia")}, and \code{vignette("rust")}.
}
\seealso{
Useful links:
\itemize{
  \item \url{https://github.com/Pierre9344/tarpolyglot}
  \item \url{https://pierre9344.github.io/tarpolyglot/}
  \item Report bugs at \url{https://github.com/Pierre9344/tarpolyglot/issues}
}

}
\author{
\strong{Maintainer}: Pierre Solomon \email{pierre.solomon@laposte.net} (\href{https://orcid.org/0000-0001-7187-2664}{ORCID})

Authors:
\itemize{
  \item Pierre Solomon \email{pierre.solomon@laposte.net} (\href{https://orcid.org/0000-0001-7187-2664}{ORCID})
}

}
\keyword{internal}
