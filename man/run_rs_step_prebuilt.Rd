% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run-step-rs.R
\name{run_rs_step_prebuilt}
\alias{run_rs_step_prebuilt}
\title{Run a Rust step from a pre-compiled library (worker behind tarpolyglot_map)}
\usage{
run_rs_step_prebuilt(
  lib,
  post_script = NULL,
  inputs = list(),
  output = "object",
  files = NULL
)
}
\arguments{
\item{lib}{A \code{tp_rust_lib} bundle from \code{\link[=compile_rs_lib]{compile_rs_lib()}} (supplied by the companion \verb{<name>_rust_lib} target).}

\item{post_script}{Path to an R script evaluated after compilation. The compiled Rust functions and the named \code{inputs} are in scope; its last expression is the target value (object mode), or it returns a character vector of file paths (file mode). Required for object mode.}

\item{inputs}{Named character vector (or list) mapping the name seen inside the step (in the R environment and, after the hand-off, in the foreign session) to the name of an upstream target, e.g. \code{c(x = "prepared_x")}. Each upstream target becomes a dependency of this target and is bound by that name in the step environment; under dynamic branching the per-branch slice is bound instead.}

\item{output}{Output mode: \code{"object"} (default) returns a converted R object, \code{"file"} returns a character vector of file paths (and defaults \code{format} to \code{"file"}).}

\item{files}{Optional character vector of file paths to return when no \code{post_script} is supplied in file mode.}
}
\value{
The value of the post-script (object mode) or a character vector of normalised file paths (file mode).
}
\description{
Reloads a compiled Rust library produced by \code{\link[=compile_rs_lib]{compile_rs_lib()}} (writing the embedded shared library to a temporary file and \code{dyn.load()}-ing it, then binding the generated wrapper functions), then evaluates the R \strong{post-script} exactly as \code{\link[=run_rs_step]{run_rs_step()}} does, with the compiled functions and the named \code{inputs} in scope. This is the function each branch target built by \code{tar_target_rs(..., pattern = tarpolyglot_map(...))} calls; it is exported so the call resolves at run time, but package users should not call it directly.
}
\details{
No Rust toolchain is needed here: reloading is a \code{dyn.load()}, not a build. See \code{\link[=tarpolyglot_map]{tarpolyglot_map()}} for the overall design.
}
\examples{
\dontrun{
# Normally invoked by tar_target_rs(pattern = tarpolyglot_map(...)).
lib <- compile_rs_lib(script = "scripts/square.rs")
run_rs_step_prebuilt(lib = lib, inputs = list(x = 21), post_script = "scripts/post.R")
}
}
\seealso{
\code{\link[=tarpolyglot_map]{tarpolyglot_map()}}, \code{\link[=compile_rs_lib]{compile_rs_lib()}}, \code{\link[=run_rs_step]{run_rs_step()}}
}
\keyword{internal}
