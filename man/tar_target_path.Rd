% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{tar_target_path}
\alias{tar_target_path}
\title{Track a script argument as a \code{targets} dependency}
\usage{
tar_target_path(name)
}
\arguments{
\item{name}{Character string, the name of an upstream target whose value is the script's file path (e.g. a \code{tar_target(..., format = "file")}).}
}
\value{
An object marking \code{name} for dependency-wiring by the \verb{tar_target_*} constructors.
}
\description{
Use as the \code{script}, \code{pre_script}, or \code{post_script} argument of \code{\link[=tar_target_py]{tar_target_py()}}/\code{\link[=tar_target_jl]{tar_target_jl()}}/\code{\link[=tar_target_rs]{tar_target_rs()}} (and their \verb{_raw} forms) instead of a literal path string, to make that step re-run whenever the script file changes. \code{name} is the name of an upstream \code{targets} target (typically one created with \code{format = "file"} tracking the script file), and its \emph{value} (the file path) is substituted in when the pipeline runs.
}
\details{
This mirrors how \code{inputs = c(x = "some_target")} already wires an upstream target in by name: the target's name is given as a string, and the constructor turns it into a real dependency. A plain string passed directly as \code{script}/\code{pre_script}/\code{post_script} keeps meaning what it always has (an untracked literal path), so existing pipelines are unaffected.
}
\examples{
# Building a target does not run it, so this example needs no Python.
# python/fit_step.py:
#   result = sum(x)
# scripts/pre.R:
#   to_py <- list(x = x)
list(
  targets::tar_target(fit_pyscript, "python/fit_step.py", format = "file"),
  tar_target_py(
    name = fit,
    script = tar_target_path("fit_pyscript"),  # re-runs when the file changes
    pre_script = "scripts/pre.R",              # untracked literal path
    inputs = c(x = "prepared_x"),
    retrieve = "result"
  )
)
}
\seealso{
\code{\link[=tar_target_py]{tar_target_py()}}, \code{\link[=tar_target_jl]{tar_target_jl()}}, \code{\link[=tar_target_rs]{tar_target_rs()}}
}
