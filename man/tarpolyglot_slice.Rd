% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pattern.R
\name{tarpolyglot_slice}
\alias{tarpolyglot_slice}
\title{Dynamic-branching slice that compiles Rust once}
\usage{
tarpolyglot_slice(..., index)
}
\arguments{
\item{...}{Upstream target(s) to branch over, with the same meaning as in the \pkg{targets} \code{slice()} pattern.}

\item{index}{Integer vector of branch indices to keep, as in the \pkg{targets} \code{slice()} pattern.}
}
\value{
This function is a marker consumed by the tarpolyglot constructors and is not meant to be evaluated on its own; calling it directly raises an error.
}
\description{
A drop-in replacement for the \pkg{targets} \code{\link[targets:tar_target]{targets::tar_target()}} pattern helper \code{slice()}, for use unquoted in the \code{pattern} argument of a tarpolyglot constructor. Like \code{\link[=tarpolyglot_map]{tarpolyglot_map()}}, on \code{\link[=tar_target_rs]{tar_target_rs()}} it compiles the extendr crate \strong{once} in a companion \verb{<name>_rust_lib} target and reuses it across the selected branches; on \code{\link[=tar_target_py]{tar_target_py()}} / \code{\link[=tar_target_jl]{tar_target_jl()}} it is exactly \code{slice()}. See \code{\link[=tarpolyglot_map]{tarpolyglot_map()}} for the full explanation and the compile-once mechanics.
}
\examples{
\dontrun{
tarpolyglot::tar_target_rs(
  name = rs_some, script = "square.rs", inputs = c(x = "vals"),
  post_script = "post.R", pattern = tarpolyglot_slice(vals, index = c(1, 3))
)
}
}
\seealso{
\code{\link[=tarpolyglot_map]{tarpolyglot_map()}}, \code{\link[=tar_target_rs]{tar_target_rs()}}, \code{\link[=tar_target_py]{tar_target_py()}}, \code{\link[=tar_target_jl]{tar_target_jl()}}
}
