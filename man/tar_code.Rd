% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tar-code.R
\name{tar_code}
\alias{tar_code}
\title{Inline code for a tarpolyglot step}
\usage{
tar_code(x)
}
\arguments{
\item{x}{Inline code: an R \code{{...}} block, or an expression/variable/literal that
yields a single character string of source code.}
}
\value{
A classed marker (\code{tp_inline}) recognised by the \verb{tar_target_*} constructors.
}
\description{
Use in place of a file-path string (or a \code{\link[=tar_target_path]{tar_target_path()}} reference) for the
\code{script}, \code{pre_script}, or \code{post_script} argument of the \code{tar_target_py()} /
\code{tar_target_jl()} / \code{tar_target_rs()} constructors (and their \verb{_raw} forms), to
supply the code inline instead of pointing at a file.
}
\details{
\code{tar_code()} accepts two forms. An R \code{{...}} block is captured as inline R code and is only valid in an R slot (\code{pre_script} or \code{post_script}); passing a block as the foreign \code{script} is an error. Anything that evaluates to a single character string is treated as inline source code, and its language follows from the slot it fills: the foreign \code{script} slot is Python, Julia, or Rust source, while a \code{pre_script} or \code{post_script} slot is R source.

Multi-line strings are supported and preserved verbatim, then \strong{dedented}: the
common leading-whitespace margin shared by all lines is stripped (and leading/
trailing blank lines dropped), so code you indent to line up with the surrounding
\verb{_targets.R} still starts flush-left. This matters for Python, whose top-level
indentation is significant; Julia/Rust/R are unaffected either way. For Python or
regex payloads, prefer an R raw string \code{r"( ... )"} so backslashes and quotes need
no escaping.

Because the inline code is embedded in the target's command, \code{targets} hashes it:
editing inline code re-runs the target automatically (unlike a literal path string,
which is untracked).
}
\examples{
# Building a target does not run it, so these examples need no Python.
# Inline R (pre/post) plus a one-line inline Python `script`:
tarpolyglot::tar_target_py(
  name        = m,
  inputs      = c(x = "prepared_x"),
  pre_script  = tar_code({ to_py <- list(x = x) }),   # inline R
  script      = tar_code("result = float(sum(x))"),   # inline Python
  post_script = tar_code({ py_get("result") })
)

# Multi-line inline Python. Indent the code to line up with `_targets.R`;
# tar_code() dedents it, so Python's own block indentation stays correct.
tarpolyglot::tar_target_py(
  name   = pos_sum,
  inputs = c(x = "prepared_x"),
  script = tar_code(r"(
    result = 0
    for v in x:
        if v > 0:
            result += v
  )")
)
}
\seealso{
\code{\link[=tar_target_path]{tar_target_path()}}, \code{\link[=tar_target_py]{tar_target_py()}}, \code{\link[=tar_target_jl]{tar_target_jl()}}, \code{\link[=tar_target_rs]{tar_target_rs()}}
}
