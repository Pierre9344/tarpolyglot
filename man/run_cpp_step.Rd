% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run-step-cpp.R
\name{run_cpp_step}
\alias{run_cpp_step}
\title{Execute a C++ step (worker behind tar_target_cpp)}
\usage{
run_cpp_step(
  script,
  post_script = NULL,
  inputs = list(),
  output = "object",
  files = NULL,
  depends = NULL,
  name = NULL
)
}
\arguments{
\item{script}{Path to the C++ script containing \verb{// [[Rcpp::export]]} functions (required). Accepts a file path or an inline \code{\link[=tar_code]{tar_code()}} carrier; see the "Script arguments" section below.}

\item{post_script}{Path to an R script evaluated after compilation. The compiled C++ functions and the named \code{inputs} are in scope; its last expression is the target value (object mode), or it returns a character vector of file paths (file mode). Required for object mode. Accepts a file path or an inline \code{\link[=tar_code]{tar_code()}} carrier; see the "Script arguments" section below.}

\item{inputs}{Named character vector (or list) mapping the name seen inside the step (in the R environment and, after the hand-off, in the foreign session) to the name of an upstream target, e.g. \code{c(x = "prepared_x")}. Each upstream target becomes a dependency of this target and is bound by that name in the step environment; under dynamic branching the per-branch slice is bound instead.}

\item{output}{Output mode: \code{"object"} (default) returns a converted R object, \code{"file"} returns a character vector of file paths (and defaults \code{format} to \code{"file"}).}

\item{files}{Optional character vector of file paths to return when no \code{post_script} is supplied in file mode.}

\item{depends}{Optional character vector of extension packages (e.g. \code{c("RcppArmadillo", "RcppEigen")}), passed straight through as \verb{// [[Rcpp::depends(...)]]} would be. Usually unnecessary: declaring \verb{// [[Rcpp::depends(pkgname)]]} directly in the C++ source (the normal Rcpp convention) already works, so this argument is only useful if you would rather not repeat that in the source itself.}

\item{name}{Character string, the step's target name. Supplied automatically by the constructor; used only to name this step's log files when \code{\link[=polyglot_controller]{polyglot_controller()}} was given a \code{\link[=tar_polyglot_log]{tar_polyglot_log()}} (\code{NULL} -- the default -- disables logging for a direct call). Only \code{Rcpp::Rcout}/\code{Rcpp::Rcerr} output from the post-script's calls into compiled code is captured; see \code{\link[=tar_polyglot_log]{tar_polyglot_log()}}.}
}
\value{
The value of the post-script (object mode) or a character vector of normalised file paths (file mode).
}
\description{
Compiles the \verb{// [[Rcpp::export]]} functions in a C++ script with \code{\link[Rcpp:sourceCpp]{Rcpp::sourceCpp()}}, exposing them as R functions in a fresh environment, then evaluates an R \strong{post-script} in that environment where you call those functions and return the result. Upstream \code{inputs} are bound in the same environment. This is the function the target built by \code{\link[=tar_target_cpp]{tar_target_cpp()}} calls; it is exported so the call resolves at run time, but package users should not call it directly.
}
\details{
Unlike Python/Julia there is \strong{no pre-script} for C++ and no live interpreter: \code{sourceCpp()} compiles a shared library and R calls into it with real type conversion (via \href{https://www.rcpp.org/}{Rcpp}). Header-only extension packages such as RcppArmadillo/RcppEigen need no special handling: declare them with a \verb{// [[Rcpp::depends(pkgname)]]} attribute directly in the C++ source, exactly as in any other Rcpp usage, and \code{sourceCpp()} picks it up. \code{sourceCpp()} compiles via R's own configured toolchain (\verb{R CMD SHLIB}), so, unlike Rust, there is no separate compiler/ABI to match -- on Windows this function still puts Rtools on \code{PATH} for the build itself, since a bare or \code{crew}-worker process may not otherwise have it there.
}
\section{Script arguments}{


A worker receives whatever the constructor already resolved, which is one of two things: a \strong{path to a file} on disk, or an \strong{inline carrier} built by \code{\link[=tar_code]{tar_code()}} that holds the code in memory. Both are accepted, so a direct call may pass either.

\code{\link[=tar_target_path]{tar_target_path()}} is deliberately \emph{not} a third form at this level. It is a constructor-level convenience: \code{\link[=tar_target_py]{tar_target_py()}} and the other constructors rewrite it while the pipeline's DAG is built, so that by the time a worker runs it has already become the ordinary file path held by the upstream target. Handing the result of \code{\link[=tar_target_path]{tar_target_path()}} straight to a worker therefore does not resolve to a file. The three forms as written in \verb{_targets.R}, and which of them tracks your edits, are covered in \code{vignette("scripts")}.
}

\examples{
\dontrun{
# Normally invoked by tar_target_cpp(); shown here as a direct call.
# scripts/square.cpp:
#   // [[Rcpp::export]]
#   double square(double x) { return x * x; }
# scripts/post.R:
#   square(x)
run_cpp_step(
  script = "scripts/square.cpp",
  inputs = list(x = 21),
  post_script = "scripts/post.R"
)
}
}
\seealso{
\code{\link[=tar_target_cpp]{tar_target_cpp()}}, \code{\link[=run_py_step]{run_py_step()}}, \code{\link[=run_jl_step]{run_jl_step()}}, \code{\link[=run_rs_step]{run_rs_step()}}
}
